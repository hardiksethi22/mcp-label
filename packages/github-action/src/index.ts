/**
 * @mcp-label/github-action
 *
 * GitHub Action entry point for CI integration.
 * Placeholder — full implementation will use @actions/core.
 */

export async function run(): Promise<void> {
  console.log('mcp-label GitHub Action — not yet implemented.');
  console.log('Use the CLI directly: npx mcp-label scan --config <path> --fail-on high');
}

