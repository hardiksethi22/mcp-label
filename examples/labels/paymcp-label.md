# MCP Safety Label

Overall grade: **F**
Overall risk: **Critical**
Static analysis only: **Yes**

| Server | Grade | Risk | Main concerns |
|---|:---:|:---:|---|
| paymcp-demo | F | Critical | secrets.env, secrets.env, secrets.env |
| paymcp-stripe-server | F | Critical | secrets.env, secrets.env, database.mutate |
| paymcp-x402 | C | High | secrets.env |
| paymcp-subscription-demo | F | Critical | secrets.env, secrets.env, payments.read |

## paymcp-demo

### Permissions

- **High:** `secrets.env` — Environment variable: OPENAI_API_KEY
- **High:** `secrets.env` — Environment variable: STRIPE_SECRET_KEY
- **High:** `secrets.env` — Environment variable: WALLEOT_API_KEY
- **Medium:** `repo.read` — Detected GitHub MCP server
- **High:** `repo.write` — Token may permit write operations
- **High:** `payments.read` — Payment service signal detected
- **Critical:** `payments.charge` — Payment charge capability detected

### Install Risks

- **Medium:** `uvx-unpinned` — The uvx package is not pinned to a specific version.

### Recommendations

- Use the least-privileged credential possible and rotate it if exposed.
- Use a fine-grained GitHub token and prefer read-only scopes unless write actions are required.
- Use restricted API keys and enable webhook verification.
- Pin package versions (e.g., package==1.2.3).

## paymcp-stripe-server

### Permissions

- **High:** `secrets.env` — Environment variable: STRIPE_API_KEY
- **High:** `secrets.env` — Environment variable: STRIPE_SECRET_KEY
- **Medium:** `database.query` — Database service signal detected
- **High:** `database.mutate` — Database access without read-only signal
- **High:** `secrets.env` — Database credential: REDIS_URL
- **High:** `payments.read` — Payment service signal detected
- **Critical:** `payments.charge` — Payment charge capability detected

### Install Risks

- **Medium:** `local-script` — The server runs a local script file.

### Recommendations

- Use the least-privileged credential possible and rotate it if exposed.
- Use read-only database credentials where possible.
- Use the least-privileged database user and rotate credentials regularly.
- Use restricted API keys and enable webhook verification.
- Review the script contents before running.

## paymcp-x402

### Permissions

- **High:** `secrets.env` — Environment variable: OPENAI_API_KEY

### Install Risks

- **Medium:** `uvx-unpinned` — The uvx package is not pinned to a specific version.

### Recommendations

- Use the least-privileged credential possible and rotate it if exposed.
- Pin package versions (e.g., package==1.2.3).

## paymcp-subscription-demo

### Permissions

- **High:** `secrets.env` — Environment variable: STRIPE_SECRET_KEY
- **High:** `secrets.env` — Environment variable: OPENAI_API_KEY
- **High:** `payments.read` — Payment service signal detected
- **Critical:** `payments.charge` — Payment charge capability detected

### Install Risks

- **Medium:** `local-script` — The server runs a local script file.

### Recommendations

- Use the least-privileged credential possible and rotate it if exposed.
- Use restricted API keys and enable webhook verification.
- Review the script contents before running.

---

*Generated by [mcp-label](https://github.com/hardiksethi22/mcp-label). This is a heuristic analysis, not a proof of safety.*